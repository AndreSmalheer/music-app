# Kopieer dit bestand naar .env en vul je eigen waarden in.
# .env wordt NOOIT gecommit.

MONGODB_URI=mongodb://localhost:27017/muziekapp
PORT=3001

# Zet op "production" op een echte server (verbergt interne foutdetails).
NODE_ENV=development

# Komma-gescheiden lijst van toegestane origins voor CORS.
# Leeg laten voor lokaal/LAN-dev (CORS staat dan open). In productie invullen,
# bv: https://app.example.com,capacitor://localhost
ALLOWED_ORIGINS=

# Optioneel — alleen nodig als je de YouTube Data API gebruikt voor zoeken/metadata
YOUTUBE_API_KEY=

# Optioneel — volledig pad naar de yt-dlp executable.
# Leeg laten als 'yt-dlp' op je PATH staat. Installeren: pip install -U yt-dlp
YTDLP_PATH=
